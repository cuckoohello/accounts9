version: '2'
services:
  accounts:
    build: .
    restart: always
    container_name: accounts
    ports:
      - 1389:1389
    external_links:
      - mongodb
networks:
  default:
    external:
      name: service
